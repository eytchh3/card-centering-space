# Local centering evaluation fixtures

Place local evaluation images for `eval_centering.py` in this folder (or nested subfolders).

Supported file types:
- `.png`
- `.jpg` / `.jpeg`
- `.webp`
- `.bmp`
- `.tif` / `.tiff`

These image files are intentionally ignored by git so binary fixtures are never committed.
